export type RatingSummaryMetric = {
  label: string;
  value: string;
};

export type RatingRegionSectionRow = {
  type: 'section';
  title: string;
};

export type RatingRegionDataRow = {
  type: 'data';
  code: string;
  name: string;
  place: number;
  value: string;
  averageRf: string;
  bestPractice: string;
};

export type RatingRegionRow = RatingRegionSectionRow | RatingRegionDataRow;

export type RatingMoMetricPair = {
  value: string;
  place: number;
};

export type RatingMoRow = {
  id: number;
  oktmo: string;
  moName: string;
  region: string;
  period: string;
  dti: RatingMoMetricPair;
  debtorsBudgetShare: RatingMoMetricPair;
  debtorsNpShare: RatingMoMetricPair;
  overpaymentNpShare: RatingMoMetricPair;
  overpaymentBudgetNpShare: RatingMoMetricPair;
  debt12mDynamics: RatingMoMetricPair;
  budgetDebt12mDynamics: RatingMoMetricPair;
  debtorsToEmployeesOver100: RatingMoMetricPair;
  budgetDebtorsToEmployeesOver100: RatingMoMetricPair;
  commissionsShare: RatingMoMetricPair;
  finalRating: number;
};

export type RatingTopMoRow = {
  oktmo: string;
  moName: string;
  rating: number;
};

export type RatingTopDebtorRow = {
  place: number;
  oktmo: string;
  name: string;
  debtAmount: number;
};

export type RatingTopDebtorSecondLevelRow = {
  place: number;
  oktmo: string;
  name: string;
  debtorsShare: string;
  debtAmount: number;
};